'use strict'
import { StyleSheet } from 'react-native';
import colors from '../../config/colors'

module.exports = StyleSheet.create({
  splashContainer: {
    backgroundColor: '#FFF', flex: 1, justifyContent: 'center', alignItems: 'center'
  },
  logoImage: {
    height: 65, width: 320
  }
});
