'use strict'

import colors from 'HSColors'
import { StyleSheet } from 'react-native'

module.exports = StyleSheet.create({
    tabBar: {
        paddingTop: 20,
        backgroundColor: "#FFF",
    },
    tabContainer: {
        flex: 1,
        paddingTop: 0,
    }
})
